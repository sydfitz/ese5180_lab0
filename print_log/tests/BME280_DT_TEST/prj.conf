CONFIG_ZTEST=y
CONFIG_ZTEST_ASSERT_VERBOSE=2

CONFIG_I2C=y
CONFIG_CBPRINTF_FP_SUPPORT=y

# Frees the TWIM1/I2C1 hardware block (shared with UARTE1) for non-secure
# use -- see print_log/prj.conf for the full explanation.
CONFIG_TFM_LOG_LEVEL_SILENCE=y
